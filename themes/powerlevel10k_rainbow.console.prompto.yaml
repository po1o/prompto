# Console variant of the `powerlevel10k_rainbow` theme, for the Linux virtual console.
#
# A console font carries no Nerd Font glyphs and the console shows 16
# colors, so this file uses ASCII separators in place of Powerline ones,
# drops the icons outright, and names its colors rather than giving hex.
# Themes that differ only in shade therefore look alike here.
#
# Edit this file directly. It is the console prompt in its own right,
# not a derived copy of powerlevel10k_rainbow.prompto.yaml: changes there do not reach
# it. Keep it to ASCII and the 16 ANSI color names, or it will not render.

aws:
    background_templates:
        - '{{if contains "default" .Profile}}lightRed{{end}}'
        - '{{if contains "jan" .Profile}}lightRed{{end}}'
    foreground: lightWhite
    options:
        display_default: false
    style: powerline
    template: ' {{ .Profile }}{{ if .Region }}@{{ .Region }}{{ end }} '
azfunc:
    background: lightYellow
    foreground: lightWhite
    options:
        display_mode: files
        fetch_version: false
    style: powerline
    template: ' {{ if .Error }}{{ .Error }}{{ else }}{{ .Full }}{{ end }} '
console_title_template: '{{ .Shell }} in {{ .Folder }}'
executiontime:
    background: yellow
    foreground: black
    style: powerline
    template: ' {{ .FormattedMs }} '
cursor_padding: true
git:
    background: green
    background_templates:
        - '{{ if or (.Working.Changed) (.Staging.Changed) }}yellow{{ end }}'
        - '{{ if and (gt .Ahead 0) (gt .Behind 0) }}lightRed{{ end }}'
        - '{{ if gt .Ahead 0 }}white{{ end }}'
        - '{{ if gt .Behind 0 }}green{{ end }}'
    foreground: black
    options:
        branch_icon: ' '
        fetch_status: true
        fetch_upstream_icon: true
        azure_devops_icon: 'az '
        bitbucket_icon: 'bb '
        cherry_pick_icon: 'pick '
        codeberg_icon: 'cb '
        codecommit_icon: 'cc '
        commit_icon: '@'
        git_icon: 'git '
        github_icon: 'gh '
        gitlab_icon: 'gl '
        merge_icon: 'merge '
        no_commits_icon: 'empty '
        rebase_icon: 'rebase '
        revert_icon: 'revert '
        tag_icon: '#'
    style: powerline
    template: ' {{ .UpstreamIcon }}{{ .HEAD }}{{if .BranchStatus }} {{ .BranchStatus }}{{ end }}{{ if .Working.Changed }} {{ .Working.String }}{{ end }}{{ if and (.Working.Changed) (.Staging.Changed) }} |{{ end }}{{ if .Staging.Changed }} {{ .Staging.String }}{{ end }}{{ if gt .StashCount 0 }} {{ .StashCount }}{{ end }} '
go:
    background: cyan
    foreground: black
    options:
        fetch_version: true
    style: powerline
    template: ' {{ if .Error }}{{ .Error }}{{ else }}{{ .Full }}{{ end }} '
julia:
    background: lightBlue
    foreground: black
    options:
        fetch_version: true
    style: powerline
    template: ' {{ if .Error }}{{ .Error }}{{ else }}{{ .Full }}{{ end }} '
node:
    background: darkGray
    foreground: lightWhite
    options:
        fetch_version: true
    style: powerline
    template: ' {{ if .PackageManagerIcon }}{{ .PackageManagerIcon }} {{ end }}{{ .Full }} '
os:
    background: white
    foreground: black
    leading_separator: --<
    template: ' {{ if .WSL }}WSL at {{ end }}{{.Icon}} '
    options:
        display_distro_name: true
        android: android
        linux: linux
        macos: mac
path:
    background: darkGray
    foreground: lightWhite
    options:
        home_icon: "~"
        style: full
        windows_registry_icon: reg
    trailing_style: powerline
    template: ' {{ .Path }} '
prompt:
    - segments:
        - os
        - path
        - git
    - segments:
        - text
python:
    background: lightYellow
    foreground: black
    options:
        display_mode: files
        fetch_virtual_env: false
    style: powerline
    template: ' {{ if .Error }}{{ .Error }}{{ else }}{{ .Full }}{{ end }} '
root:
    background: lightYellow
    foreground: black
    style: powerline
    template: ' # '
rprompt:
    - segments:
        - node
        - go
        - julia
        - python
        - ruby
        - azfunc
        - aws
        - root
        - executiontime
        - status
        - time
    - segments:
        - text.2
ruby:
    background: red
    foreground: lightWhite
    options:
        display_mode: files
        fetch_version: true
    style: powerline
    template: ' {{ if .Error }}{{ .Error }}{{ else }}{{ .Full }}{{ end }} '
status:
    background: black
    background_templates:
        - '{{ if gt .Code 0 }}red{{ end }}'
    foreground: white
    options:
        always_enabled: true
    style: powerline
    template: ' {{ if gt .Code 0 }}{{ reason .Code }}{{ else }}{{ end }} '
text:
    foreground: white
    template: --
text.2:
    foreground: white
    template: --
time:
    background: white
    foreground: black
    leading_separator: '>--'
    template: ' {{ .LastDate | date .Format }} '
    trailing_style: powerline
