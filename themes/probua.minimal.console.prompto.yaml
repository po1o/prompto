# Code generated by src/tools/genconsolethemes; DO NOT EDIT.
#
# Console variant of "probua.minimal", for the Linux virtual console.
#
# Nerd Font icons are removed and Powerline separators replaced with ASCII,
# because a console font carries neither. Colors are approximated to the 16
# ANSI colors a console can show, so themes that differ only in shade look
# alike here.
#
# To take this file over, delete the generated marker on the first line: the
# generator then leaves it alone. Otherwise edit probua.minimal.prompto.yaml and regenerate.

git:
    options:
        branch_icon: ""
        fetch_status: true
        azure_devops_icon: 'az '
        bitbucket_icon: 'bb '
        cherry_pick_icon: 'pick '
        codeberg_icon: 'cb '
        codecommit_icon: 'cc '
        commit_icon: '@'
        git_icon: 'git '
        github_icon: 'gh '
        gitlab_icon: 'gl '
        merge_icon: 'merge '
        no_commits_icon: 'empty '
        rebase_icon: 'rebase '
        revert_icon: 'revert '
        tag_icon: '#'
    template: git:{{ if or (.Working.Changed) (.Staging.Changed) (gt .StashCount 0) }}<lightYellow>{{ .HEAD }}</>{{ else }}{{ .HEAD }}{{ end }}{{ if .Staging.Changed }} <white>{{ .Staging.String }}</>{{ end }}{{ if .Working.Changed }} <lightRed>{{ .Working.String }}</>{{ end }}
path:
    foreground: lightBlue
    options:
        style: full
        windows_registry_icon: reg
    template: '{{ .Path }} '
prompt:
    - segments:
        - session
        - path
        - git
    - segments:
        - status
session:
    foreground: lightYellow
    template: '{{ .UserName }}@{{ .HostName }} '
status:
    foreground: lightYellow
    foreground_templates:
        - '{{ if gt .Code 0 }}lightBlue{{ end }}'
    options:
        always_enabled: true
    template: '> '
