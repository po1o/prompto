# Code generated by src/tools/genconsolethemes; DO NOT EDIT.
#
# Console variant of "half-life", for the Linux virtual console.
#
# Nerd Font icons are removed and Powerline separators replaced with ASCII,
# because a console font carries neither. Colors are approximated to the 16
# ANSI colors a console can show, so themes that differ only in shade look
# alike here.
#
# To take this file over, delete the generated marker on the first line: the
# generator then leaves it alone. Otherwise edit half-life.prompto.yaml and regenerate.

git:
    foreground: lightCyan
    options:
        branch_ahead_icon: ""
        branch_behind_icon: ""
        branch_gone_icon: ""
        branch_icon: ""
        branch_identical_icon: ""
        cherry_pick_icon: 'pick '
        commit_icon: '@'
        fetch_status: true
        merge_icon: 'merge '
        rebase_icon: 'rebase '
        revert_icon: 'revert '
        tag_icon: '#'
        azure_devops_icon: 'az '
        bitbucket_icon: 'bb '
        codeberg_icon: 'cb '
        codecommit_icon: 'cc '
        git_icon: 'git '
        github_icon: 'gh '
        gitlab_icon: 'gl '
        no_commits_icon: 'empty '
    template: '<lightWhite>on</> {{ .HEAD }}{{ if .Staging.Changed }}<lightGreen> * {{ .Staging.String }}</>{{ end }}{{ if .Working.Changed }}<yellow> * {{ .Working.String }}</>{{ end }} '
path:
    foreground: lightGreen
    options:
        style: full
        windows_registry_icon: reg
    template: '{{ .Path }} '
prompt:
    - segments:
        - session
        - text
        - path
        - git
        - text.2
session:
    foreground: lightBlue
    template: '{{ .UserName }} '
text:
    foreground: lightWhite
    template: 'in '
text.2:
    foreground: yellow
    template: 'l '
