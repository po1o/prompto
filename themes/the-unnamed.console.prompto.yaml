# Code generated by src/tools/genconsolethemes; DO NOT EDIT.
#
# Console variant of "the-unnamed", for the Linux virtual console.
#
# Nerd Font icons are removed and Powerline separators replaced with ASCII,
# because a console font carries neither. Colors are approximated to the 16
# ANSI colors a console can show, so themes that differ only in shade look
# alike here.
#
# To take this file over, delete the generated marker on the first line: the
# generator then leaves it alone. Otherwise edit the-unnamed.prompto.yaml and regenerate.

git:
    foreground: lightRed
    options:
        branch_icon: ' '
        azure_devops_icon: 'az '
        bitbucket_icon: 'bb '
        cherry_pick_icon: 'pick '
        codeberg_icon: 'cb '
        codecommit_icon: 'cc '
        commit_icon: '@'
        git_icon: 'git '
        github_icon: 'gh '
        gitlab_icon: 'gl '
        merge_icon: 'merge '
        no_commits_icon: 'empty '
        rebase_icon: 'rebase '
        revert_icon: 'revert '
        tag_icon: '#'
    template: '<lightWhite>on</> {{ .HEAD }}{{if .BranchStatus }} {{ .BranchStatus }}{{ end }}{{ if .Working.Changed }} {{ .Working.String }}{{ end }}{{ if and (.Working.Changed) (.Staging.Changed) }} |{{ end }}{{ if .Staging.Changed }} {{ .Staging.String }}{{ end }} '
node:
    foreground: lightCyan
    options:
        fetch_version: true
    template: '<lightWhite>via</> {{ .Full }} '
npm:
    foreground: lightRed
    template: '<lightWhite>and</> {{.Full}} '
path:
    foreground: cyan
    options:
        style: folder
        windows_registry_icon: reg
    template: '{{ .Path }} '
project:
    foreground: white
    template: '{{if .Version}}<lightWhite>is</> {{.Version}} {{end}}'
prompt:
    - segments:
        - path
        - git
        - project
        - node
        - npm
    - segments:
        - text
text:
    foreground: lightYellow
    template: '> '
