# Code generated by src/tools/genconsolethemes; DO NOT EDIT.
#
# Console variant of "json", for the Linux virtual console.
#
# Nerd Font icons are removed and Powerline separators replaced with ASCII,
# because a console font carries neither. Colors are approximated to the 16
# ANSI colors a console can show, so themes that differ only in shade look
# alike here.
#
# To take this file over, delete the generated marker on the first line: the
# generator then leaves it alone. Otherwise edit json.prompto.yaml and regenerate.

cursor_padding: true
git:
    foreground: lightCyan
    options:
        branch_icon: ""
        azure_devops_icon: 'az '
        bitbucket_icon: 'bb '
        cherry_pick_icon: 'pick '
        codeberg_icon: 'cb '
        codecommit_icon: 'cc '
        commit_icon: '@'
        git_icon: 'git '
        github_icon: 'gh '
        gitlab_icon: 'gl '
        merge_icon: 'merge '
        no_commits_icon: 'empty '
        rebase_icon: 'rebase '
        revert_icon: 'revert '
        tag_icon: '#'
    template: <lightWhite> ! </><b>{{ .HEAD }}</b>
path:
    foreground: lightYellow
    options:
        style: folder
        windows_registry_icon: reg
    template: <lightGreen> </><b>{{ .Path }}</b>
prompt:
    - segments:
        - path
        - git
        - text
    - segments:
        - text.2
text:
    foreground: red
    template: <red> *</>
text.2:
    foreground: lightWhite
    template: '>'
