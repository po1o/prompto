# Code generated by src/tools/genconsolethemes; DO NOT EDIT.
#
# Console variant of "xtoys", for the Linux virtual console.
#
# Nerd Font icons are removed and Powerline separators replaced with ASCII,
# because a console font carries neither. Colors are approximated to the 16
# ANSI colors a console can show, so themes that differ only in shade look
# alike here.
#
# To take this file over, delete the generated marker on the first line: the
# generator then leaves it alone. Otherwise edit xtoys.prompto.yaml and regenerate.

git:
    foreground: lightCyan
    options:
        branch_icon: ""
        fetch_upstream_icon: false
        azure_devops_icon: 'az '
        bitbucket_icon: 'bb '
        cherry_pick_icon: 'pick '
        codeberg_icon: 'cb '
        codecommit_icon: 'cc '
        commit_icon: '@'
        git_icon: 'git '
        github_icon: 'gh '
        gitlab_icon: 'gl '
        merge_icon: 'merge '
        no_commits_icon: 'empty '
        rebase_icon: 'rebase '
        revert_icon: 'revert '
        tag_icon: '#'
    template: 'HEAD:{{ .UpstreamIcon }}{{ .HEAD }} '
path:
    foreground: lightBlue
    options:
        style: agnoster_short
        windows_registry_icon: reg
    template: '{{ .Path }} '
prompt:
    - segments:
        - root
        - session
        - path
        - git
        - status
root:
    foreground: lightYellow
    template: '# '
session:
    template: '{{ .UserName }}@{{ .HostName }} '
status:
    foreground: lightCyan
    foreground_templates:
        - '{{ if gt .Code 0 }}lightRed{{ end }}'
    options:
        always_enabled: true
    template: '<white>></><lightCyan>></><foreground>></> '
